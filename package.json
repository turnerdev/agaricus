{
  "name": "agaricus",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "tests": "yarn lerna -- run test --parallel --no-bail -- --passWithNoTests --runInBand",
    "start": "yarn lerna -- run start --parallel --no-bail"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
